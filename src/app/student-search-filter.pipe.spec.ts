import { StudentSearchFilterPipe } from './student-search-filter.pipe';

describe('StudentSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
